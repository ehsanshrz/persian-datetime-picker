## [2.0.0] - Update to Flutter 3 / Dart 3 (null-safety, modern APIs).
* Migrated to Dart 3 null-safety (SDK `>=3.0.0 <4.0.0`).
* Replaced deprecated `FlatButton` with `TextButton`.
* Replaced deprecated `RaisedButton` with `ElevatedButton` in example.
* Removed `flushbar` dependency; replaced with Flutter built-in `SnackBar`.
* Updated `shamsi_date` to `^1.1.1`.
* Replaced custom `CDialog` internals with Flutter's built-in `Dialog` widget.
* Fixed all null-safety issues across the codebase.
## [0.0.1] - TODO: Add release date.
## [1.0.0] - Add year picker and month picker, add disable feature,fix bugs.
## [1.0.1] - Add theme color ,fix bugs.
* TODO: Describe initial release.
